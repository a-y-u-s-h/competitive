#include <bits/stdc++.h>

int main(int argc, char const *argv[]) {
  int T;
  std::cin >> T;
  for (int t = 0 ; t < T; t += 1) {
    int X;
    std::cin >> X;
    /*
      ======================================
        You're given a positive integer X.
        Your task is to tell whether there
        exist two positive integers A and B
        (A > 0, B > 0) such that:

        2A + 2B + AB = X

        If there exist positive integers A
        and B satisfying the above condition
        print YES, otherwise print NO.
      ======================================
    */
  }
  return 0;
}
