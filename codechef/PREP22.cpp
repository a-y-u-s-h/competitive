#include <bits/stdc++.h>

int main(int argc, char const *argv[]) {
  int T;
  std::cin >> T;
  for (int t = 0 ; t < T; t += 1) {
    int N;
    std::cin >> N;
    std::vector<int> A;
    for (int n = 0; n < N; n++) { int Ai; std::cin >> Ai; A.push_back(Ai); }
    /*
      ======================================
        You're given an array A1, A2...An of
        length N. A represents a histogram
        which mean the height of the i-th
        bar will be Ai units and the width
        will be 1 unit. Find the area of
        the largest rectangle in histogram.
      ======================================
    */
  }
  return 0;
}
