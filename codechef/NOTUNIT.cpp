#include <bits/stdc++.h>

int main(int argc, char const *argv[]) {
  int T;
  std::cin >> T;
  for (int t = 0 ; t < T; t += 1) {
    long long A, B;
    std::cin >> A >> B;
    /*
      ======================================
        Given two positive integers A and B,
        find two positive integers i and j
        such that -

        1) GCD(i, j) > 1.
        2) A <= i < j <= B.
        3) The value (i + j) is minimum possible.

        If there are multiple solutions, you
        may print any of them. If no such pair
        exists, print -1.
      ======================================
    */

  }
  return 0;
}
