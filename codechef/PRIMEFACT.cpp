#include <bits/stdc++.h>

int main(int argc, char const *argv[]) {
  int T;
  std::cin >> T;
  for (int t = 0 ; t < T; t += 1) {
    int X, Y;
    std::cin >> X >> Y;
    /*
      ======================================
        Chef's current score is X. Each
        second, Chef will find the smallest
        prime factor of his score and add
        it to his score. Determine the minimum
        time in seconds, after which his score
        becomes >= Y.
      ======================================
    */
  }
  return 0;
}
