#include <bits/stdc++.h>

int main(int argc, char const *argv[]) {
  int T;
  std::cin >> T;
  for (int t = 0 ; t < T; t += 1) {
    int A, B, N;
    std::cin >> A >> B >> N;
    /*
      ======================================
        Danya gave integers A, B and N to
        Archi. Archi wants to compare A^N and
        B^N. Help Archi with this task.
      ======================================
    */
  }
  return 0;
}
